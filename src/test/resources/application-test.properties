# Test configuration
# Clear the defaults so @DynamicPropertySource in TestApplicationContext can set them
# These will be set by TestApplicationContext based on useLocalNeo4j flag
embabel.agent.rag.neo.uri=
embabel.agent.rag.neo.username=
embabel.agent.rag.neo.password=
